name: Build HPJY-HUD (.tipa / .ipa)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  THEOS: $HOME/theos
  SDK_VERSION: "15.0"
  XCODE_VERSION: "15.1"

jobs:
  build:
    runs-on: macos-13

    steps:
    - name: 🧩 Checkout repository
      uses: actions/checkout@v4

    - name: ⚙️ Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: ${{ env.XCODE_VERSION }}

    - name: 📦 Install build dependencies (with Swift support)
      run: |
        brew install make dpkg ldid git swift
        rm -rf $HOME/theos || true
        git clone --recursive --recurse-submodules -j8 https://github.com/kabiroberai/theos $HOME/theos
        cd $HOME/theos
        git submodule sync
        git submodule update --init --recursive --jobs 8
        rm -rf $HOME/theos/sdks || true
        git clone https://github.com/theos/sdks $HOME/theos/sdks
        echo "THEOS=$HOME/theos" >> $GITHUB_ENV
        echo "PATH=$HOME/theos/bin:$PATH" >> $GITHUB_ENV

    - name: 🧱 Configure permissions
      run: |
        chmod +x ./build.sh || true
        chmod +x ./gen-control.sh || true

    - name: 🏗️ Build HPJY-HUD (Swift + ObjC++)
      run: |
        export THEOS_SWIFT_USE_PREBUILT=1
        make package FINALPACKAGE=1 \
          ARCHS="arm64 arm64e" \
          TARGET="iphone:clang:latest:${{ env.SDK_VERSION }}"
      env:
        THEOS: ${{ env.THEOS }}

    - name: 📤 Upload built .tipa / .deb
      uses: actions/upload-artifact@v4
      with:
        name: HPJY-HUD
        path: |
          packages/*.tipa
          packages/*.ipa
          packages/*.deb
